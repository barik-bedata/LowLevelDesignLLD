# HTTP Status Codes (বিস্তারিত গাইড)

ইন্টারভিউ এবং MCQ পরীক্ষার জন্য HTTP Status Code খুবই গুরুত্বপূর্ণ। সাধারণত 200, 404, 500 এগুলো সবাই জানে, কিন্তু এর ভেতরের অন্য কোডগুলো থেকে প্রশ্ন দিয়ে প্রায়ই কনফিউজ করা হয়। 

আপনার দেওয়া নির্দিষ্ট কোডগুলোর বিস্তারিত কাজ ও ব্যবহার নিচে বাংলায় ব্যাখ্যা করা হলো:

---

## 🟢 2xx: Success (সফলতা)
এই ক্লাসের কোডগুলো বোঝায় যে ক্লায়েন্টের রিকোয়েস্ট সার্ভার সফলভাবে গ্রহণ করেছে এবং প্রসেস করেছে।

*   **`200 OK`:** সবচেয়ে সাধারণ সাকসেস কোড। এর মানে রিকোয়েস্টটি সফল হয়েছে এবং সার্ভার কাঙ্ক্ষিত রেসপন্স (যেমন ডাটা বা পেজ) পাঠিয়ে দিয়েছে। (সাধারণত GET, PUT রিকোয়েস্টে ব্যবহৃত হয়)।
*   **`201 Created`:** রিকোয়েস্টটি সফল হয়েছে এবং এর ফলে সার্ভারে নতুন একটি ডাটা বা রিসোর্স তৈরি হয়েছে। (সাধারণত POST রিকোয়েস্টের পর দেওয়া হয়)।
*   **`202 Accepted`:** রিকোয়েস্টটি প্রসেস করার জন্য সার্ভার গ্রহণ করেছে, কিন্তু প্রসেসিং এখনো শেষ হয়নি। এটি সাধারণত ব্যাকগ্রাউন্ড বা Asynchronous টাস্কের ক্ষেত্রে দেওয়া হয় (যেমন একটি বড় ফাইল প্রসেস করা)।
*   **`203 Non-Authoritative Information`:** রিকোয়েস্ট সফল, কিন্তু রেসপন্সের ডাটা মূল (Original) সার্ভার থেকে সরাসরি আসেনি। ডাটাটি অন্য কোনো লোকাল প্রক্সি বা থার্ড-পার্টি সোর্স থেকে মডিফাই হয়ে এসেছে।
*   **`204 No Content`:** রিকোয়েস্টটি পুরোপুরি সফল, কিন্তু সার্ভারের রেসপন্স হিসেবে ক্লায়েন্টকে পাঠানোর মতো কোনো বডি বা ডাটা নেই। (সাধারণত DELETE রিকোয়েস্ট সফল হলে এটি ব্যবহৃত হয়)।
*   **`205 Reset Content`:** রিকোয়েস্ট সফল। সার্ভার ক্লায়েন্টকে নির্দেশ দিচ্ছে তার বর্তমান ভিউ বা স্ক্রিনটি (যেমন ফর্মের ইনপুট ফিল্ডগুলো) ক্লিয়ার বা রিসেট করে ফেলতে, যাতে নতুন ইনপুট দেওয়া যায়।

---

## 🟡 3xx: Redirection (রিডাইরেকশন বা অন্য ঠিকানায় পাঠানো)
এই কোডগুলো বোঝায় যে, ক্লায়েন্টের রিকোয়েস্টটি সম্পন্ন করতে হলে তাকে আরও কিছু কাজ করতে হবে, সাধারণত অন্য কোনো URL-এ যেতে হবে।

*   **`300 Multiple Choices`:** ক্লায়েন্ট যে রিসোর্স চেয়েছে তার একাধিক অপশন আছে। ক্লায়েন্টকে যেকোনো একটি বেছে নিতে হবে। (যেমন একটি ভিডিওর .mp4 এবং .avi দুটি ভার্সন আছে)।
*   **`301 Moved Permanently`:** ক্লায়েন্ট যে URL-এ রিকোয়েস্ট করেছে, সেটি চিরস্থায়ীভাবে নতুন একটি URL-এ মুভ হয়ে গেছে। রেসপন্সে নতুন URL টি দেওয়া থাকে এবং ব্রাউজার স্বয়ংক্রিয়ভাবে সেখানে চলে যায়। (SEO এর জন্য এটি খুব জরুরি)।
*   **`302 Found (Moved Temporarily)`:** রিসোর্সটি সাময়িকভাবে অন্য একটি URL-এ পাওয়া যাচ্ছে। তবে ভবিষ্যতে আবার পুরনো URL-টি কাজ করতে পারে।
*   **`303 See Other`:** ক্লায়েন্ট যে ডাটা পাঠিয়েছে তা প্রসেস হয়েছে, এখন ক্লায়েন্টকে একটি GET রিকোয়েস্ট ব্যবহার করে অন্য একটি URL থেকে রেজাল্ট দেখতে বলা হচ্ছে। (সাধারণত ফর্ম সাবমিট করার পর পেজ রিফ্রেশ দিলে যেন ডাবল সাবমিট না হয় (Double-submit problem), তা আটকাতে এটি ব্যবহৃত হয়)।
*   **`304 Not Modified`:** সার্ভার ক্লায়েন্টকে বলছে, "তুমি লাস্ট বার আমার কাছ থেকে যে ডাটা নিয়ে ক্যাশে (Cache) সেভ করে রেখেছো, তা এখনো চেঞ্জ হয়নি।" ফলে ক্লায়েন্ট সার্ভার থেকে ডাটা ডাউনলোড না করে তার লোকাল ক্যাশ থেকেই ডাটা দেখায়। এটি পারফরম্যান্স অনেক বাড়িয়ে দেয়।
*   **`305 Use Proxy` (Deprecated):** ক্লায়েন্টকে বলা হচ্ছে যে রিকোয়েস্টটি সরাসরি সার্ভারে না পাঠিয়ে একটি নির্দিষ্ট প্রক্সির মাধ্যমে পাঠাতে হবে। (সিকিউরিটির কারণে এটি এখন আর আধুনিক ব্রাউজারে ব্যবহৃত হয় না)।

---

## 🟠 4xx: Client Error (ক্লায়েন্টের ভুল)
রিকোয়েস্ট সম্পন্ন না হওয়ার পেছনে ক্লায়েন্টের (যেমন ব্রাউজার বা ইউজারের) কোনো ভুল আছে।

*   **`400 Bad Request`:** ক্লায়েন্টের পাঠানো রিকোয়েস্টের সিনট্যাক্স, প্যারামিটার বা ডাটা ফরম্যাটে ভুল আছে যা সার্ভার বুঝতে পারছে না। (যেমন JSON ফরম্যাটে ব্র্যাকেট মিসিং)।
*   **`401 Unauthorized`:** ক্লায়েন্ট কে, তা সার্ভার জানে না। অর্থাৎ ইউজার লগ-ইন করেনি বা সঠিক Authentication টোকেন (যেমন JWT) দেয়নি।
*   **`402 Payment Required`:** এটি মূলত ভবিষ্যতের জন্য রিজার্ভ করে রাখা হয়েছিল। তবে বর্তমানে কিছু API (যেমন Stripe বা YouTube API) পেমেন্ট লিমিট শেষ হলে বা সাবস্ক্রিপশন না থাকলে এটি ব্যবহার করে।
*   **`403 Forbidden`:** সার্ভার জানে ক্লায়েন্ট কে (লগ-ইন করা আছে), কিন্তু ক্লায়েন্টের ওই কাজটি করার বা ওই পেজটি দেখার কোনো অধিকার (Permission/Authorization) নেই। (যেমন একজন সাধারণ ইউজার যদি এডমিন প্যানেলে ঢুকতে চায়)।
*   **`404 Not Found`:** ক্লায়েন্ট যে রিসোর্স বা URL খুঁজছে, তা সার্ভারে নেই।
*   **`405 Method Not Allowed`:** ক্লায়েন্ট যে HTTP মেথড (GET, POST ইত্যাদি) ব্যবহার করেছে, তা ওই নির্দিষ্ট URL-এর জন্য অ্যালাউড নয়। (যেমন, ডাটা রিড করার একটি API-তে কেউ POST রিকোয়েস্ট পাঠালে সার্ভার এটি দেবে)।

---

## 🔴 5xx: Server Error (সার্ভারের ভুল)
ক্লায়েন্টের রিকোয়েস্ট ঠিক ছিল, কিন্তু সার্ভারের অভ্যন্তরীণ কোনো সমস্যার কারণে সে রিকোয়েস্ট প্রসেস করতে পারেনি।

*   **`500 Internal Server Error`:** সবচেয়ে সাধারণ সার্ভার এরর। সার্ভারের কোডে (ব্যাকএন্ডে) এমন কোনো অপ্রত্যাশিত সমস্যা, বাগ বা এক্সেপশন (Exception) হয়েছে যা সে সামলাতে পারেনি।
*   **`501 Not Implemented`:** ক্লায়েন্ট এমন কোনো রিকোয়েস্ট বা মেথড (যেমন PATCH) ব্যবহার করেছে যা সার্ভারের কোডে এখনো ইমপ্লিমেন্ট বা তৈরি করা হয়নি।
*   **`502 Bad Gateway`:** যখন আপনার সার্ভারটি একটি প্রক্সি বা গেটওয়ে (যেমন Nginx) হিসেবে কাজ করছে, এবং সে পেছনের মূল সার্ভার থেকে একটি ইনভ্যালিড (ভুল) রেসপন্স পেয়েছে।
*   **`503 Service Unavailable`:** সার্ভারটি বর্তমানে অতিরিক্ত ওভারলোডেড (Traffic বেশি) অথবা মেইনটেনেন্স বা আপডেটের কারণে ডাউন আছে, তাই সে সাময়িকভাবে রিকোয়েস্ট নিতে পারছে না।
*   **`504 Gateway Timeout`:** প্রক্সি বা গেটওয়ে সার্ভার নির্দিষ্ট সময়ের (Timeout Limit) মধ্যে পেছনের মূল সার্ভার (বা অন্য কোনো সার্ভার) থেকে কোনো রেসপন্স পায়নি।
*   **`505 HTTP Version Not Supported`:** ক্লায়েন্ট এমন একটি HTTP ভার্সন (যেমন HTTP/3) ব্যবহার করে রিকোয়েস্ট করেছে, যা সার্ভার সাপোর্ট করে না।
